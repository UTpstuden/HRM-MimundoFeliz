package com.utp.hcm.repository;

import com.utp.hcm.model.TipoPension;
import org.springframework.data.jpa.repository.JpaRepository;

public interface TipoPensionRepository extends JpaRepository<TipoPension, Integer> {
}
